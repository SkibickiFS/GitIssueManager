# # # # # # # # # # # # # #
# Ignorowane przez Git dla projektów Visual Studio / .NET Core
# Wygenerowane na podstawie szablonu GitHub: https://github.com/github/gitignore/blob/main/VisualStudio.gitignore
# oraz dostosowane do projektu.
# # # # # # # # # # # # # #

# Pliki specyficzne dla użytkownika
*.rsuser
*.suo
*.user
*.userosscache
*.sln.docstates

# Pliki specyficzne dla użytkownika (MonoDevelop/Xamarin Studio)
*.userprefs

# Wyniki kompilacji
## Katalogi wyjściowe kompilacji
[Bb]in/
[Oo]bj/

## Narzędzia kompilacji .NET Core
project.lock.json
project.fragment.lock.json
artifacts/

# Pliki generowane przez Visual Studio
## Katalog cache/opcji Visual Studio (wszystkie wersje)
.vs/

## Nie śledź plików w folderze wwwroot, jeśli są generowane dynamicznie
## (Odkomentuj, jeśli dotyczy)
# **/wwwroot/

# Pliki specyficzne dla Rider (JetBrains IDE)
.idea/

# Wyniki testów
[Tt]est[Rr]esult*/
[Bb]uild[Ll]og.*
*.VisualState.xml
TestResult.xml
*.nunit
*.trx

# Wyniki kompilacji projektu ATL
[Dd]ebugPS/
[Rr]eleasePS/
dlldata.c

# Wyniki BenchmarkDotNet
BenchmarkDotNet.Artifacts/

# Pliki konfiguracyjne kompilacji .NET Core
Directory.Build.props
Directory.Build.targets

# Pliki cache ReSharper
_ReSharper*/
*.[Rr]e[Ss]harper
*.DotSettings.user

# Pliki cache JustCode
.JustCode

# Pliki generowane automatycznie przez Roslyn
Generated/

# Pliki kopii zapasowych i raportów z konwersji starych projektów
_UpgradeReport_Files/
Backup*/
UpgradeLog*.XML
UpgradeLog*.htm
ServiceFabricBackup/
*.bak

# Pliki instalatora Windows
*.cab
*.msi
*.msm
*.msp

# Pliki cache miniatur systemu Windows
Thumbs.db
Thumbs.db:encryptable
ehthumbs.db
ehthumbs_vista.db

# Pliki zrzutów pamięci (dump)
*.mdmp
*.dmp

# Pliki systemu Mac OS
.DS_Store

# Pliki generowane przez scaffolding
*.scaffolding/

# Menedżer zależności Paket
.paket/paket.exe
paket-files/

# FAKE - F# Make
.fake/

# Pliki cache CodeAnalysis
*.CodeAnalysisLog.xml

# Pliki cache StyleCop
StyleCopReport.xml
stylescop.cache

# Pakiety artefaktów AppVeyor
*.nupkg

# Pliki generowane przez Chocolatey
*.nuspec
*.choco

# Pakiety symboli MyGet
*.snupkg

# Pliki generowane automatycznie przez narzędzia (np. projektanty)
*.Generated.cs
*.designer.cs
*.generated.js
*.generated.ts

# Foldery z lokalnie zainstalowanymi narzędziami
tools/

# Pliki generowane przez polecenia migracji EF
*EFMigrationsHistory*

# Pakiety NuGet
# Pozwól na śledzenie definicji źródeł pakietów, ale ignoruj lokalny cache
*.nuget.props
*.nuget.targets

# Globalny cache NuGet v3
# https://docs.microsoft.com/pl-pl/nuget/consume-packages/managing-the-global-packages-and-cache-folders
packages/
# z wyjątkiem folderu build/, który jest używany jako cel MSBuild
!packages/build/

# Tymczasowe dla migracji użytkowników - do usunięcia po oficjalnym wsparciu IDE
*.nuget.cache

# Pliki środowiskowe
.env*

# Lokalne sekrety / konfiguracja deweloperska
# WAŻNE: Upewnij się, że pliki z produkcyjnymi sekretami są ZAWSZE ignorowane!
# Poniższa linia ignoruje appsettings.Development.json - jeśli nie zawiera on sekretów, możesz ją usunąć.
# Rozważ dodanie konkretnych nazw plików z sekretami, np. secrets.json, jeśli ich używasz.
**/appsettings.Development.json
**/secrets.json

# Plik nadpisujący konfigurację Docker Compose (często zawiera ustawienia lokalne)
**/docker-compose.override.yml
**/docker-compose.vs.debug.yml
**/docker-compose.vs.release.yml

# Pliki certyfikatów
*.pfx
*.crt
*.cer

# Pliki specyficzne dla publikacji
*.publish.xml
*.pubxml
publish/
**/PublishProfiles/